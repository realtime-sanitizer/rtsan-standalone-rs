[package]
name = "detection-tests"
edition = "2021"
publish = false

[workspace]

[dependencies]
rtsan-standalone = { path = "../../../rtsan-standalone-rs" }

[features]
rtsan = ["rtsan-standalone/enable"]
